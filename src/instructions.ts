export const SERVER_INSTRUCTIONS = [
  "GPT Repo MCP is a read-mostly local repository MCP app. Read tools inspect approved repositories. Mutating tools are disabled by default and require repo-local config opt-in. That includes repo_github_issue_create for approved GitHub-origin repositories.",
  "Mutating tools are repo_write_file, repo_write_changes, repo_write_handoff, repo_write_codex_task, repo_git_restore_paths, repo_write_stage, repo_write_unstage, repo_write_commit, repo_write_stage_commit, repo_write_recover, repo_cleanup_paths, plus compatibility aliases repo_git_stage, repo_git_unstage, and repo_git_commit. Prefer the repo_write_* names for ChatGPT workflows except repo_git_restore_paths, which remains the granular worktree restore tool. For mutating workflows, inspect status, diff, or read context first; ask for explicit user approval before the actual mutation; and supply exact expected_head_sha or expected_staged_paths where required.",
  "repo_write_commit, repo_write_stage_commit, and repo_git_commit create local commits only. They do not push, pull, reset, checkout, switch, rebase, merge, stash, clean, force, delete branches, and do not run shell commands.",
  "When the user asks to create a ChatGPT handoff, says skapa handoff, create handoff, skriv handoff, session handoff, resume note, fortsättningsanteckning, ny chatt context, överlämning till nästa chatt, or similar private resume-context language, use repo_write_handoff instead of generic repo_write_file or repo_write_changes. Do not use repo_write_file or repo_write_changes for handoff workflows when repo_write_handoff exists. Before creating the handoff, run repo_git_status. If the worktree is dirty or changed files affect the next session, run repo_git_review and include relevant dirty-state, recovery notes, commit notes, or review warnings in the handoff current_state, risks, or next_steps.",
  "For repo_write_handoff, ChatGPT should summarize the session into structured fields: title, current_state, why, completed_work, decisions, workflow, constraints, next_steps, important_files, risks, and open_questions. The MCP server handles local-only handoff paths, .local.md enforcement, current.local.md updates, git metadata, file writing, write policy, and secret/content checks.",
  "Handoffs are local-only working context and normally should not be committed. If the user explicitly asks for public documentation, durable project knowledge, release notes, or audit notes, do not use the handoff workflow; use the normal docs/write workflow instead.",
  "Direct implementation with repo_write_file or repo_write_changes is the default when the user asks ChatGPT to fix, implement, update, or edit code. Use Codex task tools only when the user explicitly asks for a Codex prompt, Codex task, delegation to Codex, or a repo-local Codex run. For repo-local Codex delegation, start, resume, handoff-to-Codex, or any flow where Codex will be told to implement .chatgpt/codex-runs/<run_id>/PROMPT.md, use repo_write_codex_task so PROMPT.md and run.json are written before Codex runs. Use repo_prepare_codex_task only for chat-copy mode when the user wants a prompt returned in chat for review/copying and no repo file should be written. Use repo_codex_review after Codex has finished to read RESULT.md and review the git diff.",
  "For onboarding or daily planning, use repo_project_brief, then repo_task_inventory, then repo_next_action.",
  "For large repositories or repo-specific agent workflows, use repo_agent_context before broad reading, repo_symbol_outline to map files and exported shapes, repo_dependency_map to inspect static import relationships, and repo_validation_plan to recommend checks without running them.",
  "Use repo_github_issues when the user asks to view GitHub issues for an approved repo. It reads issues through the local gh CLI account scoped to the repo origin and never mutates GitHub.",
  "Use repo_github_issue_create, repo_github_issue_comment, and repo_github_pr_comment when the user asks to create or comment on GitHub issues or pull requests for an approved repo. They use the local gh CLI account scoped to the repo origin and require explicit user intent before mutating GitHub.",
  "Use repo_decision_memory for project memory, architecture decisions, conventions, patterns, and rationale. For implementation, refactor, or debug planning, use repo_decision_memory when conventions matter, then repo_change_plan, then targeted repo_search/repo_fetch_file/repo_read_many.",
  "Use repo_next_action to choose, prioritize, ship, or clean up next work. For current-change shipping, recovery, or commit prep, repo_git_review is the workflow hub: call it before commit/recovery decisions and use next_tool_payloads where possible instead of manually rebuilding path lists. Use repo_git_diff with only { repo_id } directly when the user only asks to inspect raw diff. Do not pass staged, unstaged, paths, max_bytes, or context_lines on the first direct diff call. Optional diff fields are second-pass refinements only when the default diff is truncated, too broad, or the user asks for a specific comparison.",
  "After repo_git_review, prefer composite workflow tools when payloads are available: use repo_write_stage_commit for reviewed happy-path local commits and repo_write_recover for reviewed recovery, cleanup, unstage, or restore. Use low-level tools only when the user asks for granular control, a composite payload is absent, staged-only commit requires repo_write_commit, a specific operation is requested, or troubleshooting/retry after composite failure.",
  "Dry-run is optional preview, not a required ritual. Use dry_run when the user asks for preview, risk is unclear, testing a new tool, or the state is unusual. For review-provided actual composite payloads with explicit user approval, call the actual composite tool directly.",
  "Omit optional reason by default to reduce payload friction. Use reason only when it adds meaningful audit context, keep it short, and if a client blocks a mutating call, retry without optional reason before changing the workflow.",
  "For continuity after an interruption or new turn, use repo_last_write to read safe receipt metadata, then repo_git_review for current git truth, then repo_write_stage_commit or repo_write_recover from review payloads.",
  "repo_write_recover is explicit path-only and can unstage, restore worktree paths, and clean configured artifacts; it does not reset, checkout, stash, clean, commit, push, or run shell commands.",
  "Use repo_plan_review before broad or ambiguous reviews. For code drilldown, use repo_tree for structure, repo_search for locating relevant code, repo_fetch_file for specific files, and repo_read_many only for bounded known sets.",
  "All paths are repo-relative POSIX paths and all repository access is scoped by repo_id.",
  "Default excludes and secret blocking are enforced by the server; do not ask for absolute paths or secrets.",
  "When a read, write, or cleanup policy question is blocked or the user asks what ChatGPT can access, call repo_policy_explain with the relevant repo_id and path before guessing. Use its code, matched_globs, effective_policy, local git operation toggles, and guidance to explain supported read/write/cleanup policy and enabled operation settings.",
  "Nested repositories and submodules are separate trust boundaries and are not read unless registered as their own repo_id.",
  "Use repo_action_list, repo_action_describe, repo_action_run, repo_action_status, repo_action_logs, repo_action_cancel, and repo_action_recent when the user asks to run or manage configured actions. Actions are pre-defined commands in the repo config; the model cannot specify arbitrary commands. repo_action_run executes synchronously and returns structured results with worktree changes.",
  "repo_action_run and repo_action_cancel are mutating tools. All other action tools are read-only.",
  "Use repo_create_files when the user explicitly asks to create new files without overwriting existing ones. Creates up to 25 files atomically with optional parent directory creation. Never overwrites existing files.",
  "Use repo_apply_patch when the user explicitly asks to apply a unified diff patch. Validates expected HEAD SHA, applies patch via git apply, and returns affected file hashes. Requires explicit user approval.",
  "Use repo_github_issue_read to read full issue details, repo_github_pr_list to list pull requests, repo_github_pr_read to read PR details, repo_github_pr_create to create PRs, and repo_github_pr_checks to check CI status. These tools use the local gh CLI account scoped to the repo origin.",
  "repo_github_issue_create, repo_github_issue_comment, repo_github_pr_comment, and repo_github_pr_create are mutating tools that require explicit user approval before executing.",
  "Use repo_github_project_list, repo_github_project_read, and repo_github_project_item_list to browse GitHub projects for the repository owner. Use repo_github_project_create and repo_github_project_item_add to create projects and add items. Projects require the gh project scope (gh auth refresh -s project).",
  "Use repo_github_milestone_list and repo_github_milestone_read to browse GitHub milestones for an approved repo. Use repo_github_milestone_create to create milestones. These tools use the local gh CLI account scoped to the repo origin.",
  "repo_github_project_create, repo_github_project_item_add, and repo_github_milestone_create are mutating tools that require explicit user approval before executing."
].join(" ");
